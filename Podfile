# Uncomment the next line to define a global platform for your project
platform :ios, '13.0' # or your target platform version

target 'AniMate' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AniMate
  pod 'Apollo', '~> 0.50.0' # Use the version you need

  target 'AniMateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AniMateUITests' do
    # Pods for testing
  end

end